---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["container", className]}>
  <slot />
</div>

<style>
  .container {
    width: min(100% - (2 * var(--site-margin)), var(--site-max-width));
    margin-inline: auto;
  }
</style>
